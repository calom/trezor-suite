{
    "name": "@trezor/request-manager",
    "version": "1.0.0",
    "private": true,
    "license": "See LICENSE.md in repo root",
    "sideEffects": false,
    "main": "lib/index",
    "scripts": {
        "lint": "eslint '**/*.{ts,tsx,js}'",
        "test:unit": "jest",
        "type-check": "tsc --build",
        "build:lib": "rimraf ./lib && tsc --build tsconfig.json"
    },
    "dependencies": {
        "socks-proxy-agent": "6.1.1"
    }
}
